//visual studio code can use "emmet" to generate html more faster.

//prueba.js

const saludar = nomber => `Hola ${nomber}`

const resultado = saludar('humberto');
const esperado = 'Hola humberto';

if ( resultado == esperado ){
    console.log('Prueba exitosa');
}
else {
    console.log('Prueba no exitosa');
}
